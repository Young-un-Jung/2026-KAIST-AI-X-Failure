# 진행 기록

이 파일은 과거 기록을 지우지 않고 아래에 계속 추가한다.

## 2026-07-17 · 경쟁작 비교 및 방향 확정

- 세 경쟁작의 공개 저장소와 보완자료 구조를 비교했다.
- 현재 작품의 가장 강한 차별점을 '이름 붙지 않은 관계와 책임의 공백'으로 정리했다.
- A/B 중 하나를 고르는 대신 서사와 인과관계, 대응 방안을 결합한 2페이지 C안을 제작했다.

## 2026-07-17 · 사이트 전면 개편

- 단순 자료 모음이던 페이지를 5단계 사건 조사 경험으로 재구성했다.
- 미완성 포스터, 스토리보드, 구버전 영상을 공개 흐름에서 제외했다.
- 실제 수행 단계에 따라 진행 상태가 갱신되도록 바꿨다.
- 최종 영상과 제안서 PDF를 사이트에 연결했다.
- 커밋 `d2cc1e6`을 `main`에 배포했다.

## 2026-07-17 · 팀 합의문 재정렬 및 출처 정비

- 팀의 최종 합의문을 콘텐츠 기준으로 다시 확정했다.
- `연결선 프로토콜`을 독립 아이디어가 아니라 합의문의 시각화 방식으로 명시했다.
- 두 번째 선을 '선택선'에서 '전환선'으로 바꿔, AI가 상담사에서 연결자로 역할을 전환한다는 합의문과 맞췄다.
- 합의되지 않은 연도별 로드맵 대신 위험의 기준·AI 역할·기존 공간 연결이라는 세 논의 기준으로 수정했다.
- 영상에 사용한 Pixabay 배경음과 효과음의 원본 URL을 기록했다.
- 작업공간과 실제 Git 저장소의 차이를 README와 작업공간 안내서에 기록했다.

## 2026-07-17 · GitHub Desktop 기준 통합 저장소 구성

- GitHub Desktop에서 클론한 `C:\Users\mynam\Documents\GitHub\2026-KAIST-AI-X-Failure`을 새로운 기준 작업공간으로 확정했다.
- 최종 합의 원문·온라인 요약·최종 PDF를 `submission/`에 모았다.
- 이전 DOCX/PDF와 보완자료 이미지를 `archive/`에 보존했다.
- Remotion 저장소의 추적 파일 51개를 `video-source/`로 통합했다.
- 원격이 없던 기존 영상 저장소의 전체 Git 이력을 `archive/git-bundles/video-history.bundle`로 백업했다.
- 픽사베이 원본 음원을 `sources/audio-originals/`에 보존했다.
- 임시 렌더링 파일, 경쟁작 클론, `node_modules`, 중첩 `.git`은 통합 대상에서 제외했다.

## 2026-07-17 · 가독성 보정 및 사건 보관소 추가

- 웹페이지 전역에 한글 단어 중간 분리를 막는 규칙과 제목 균형 줄바꿈을 적용했다.
- 최종 문장을 의미 단위 3행으로 고정해 조사와 어미가 홀로 남는 문제를 해결했다.
- 외부 Google Drive로 이탈하는 자료 목록 대신, 완성 영상·최종 제안서·영상 스틸을 묶은 `CASE ARCHIVE`를 본문에 추가했다.
- 작업 화면이 보이는 포스터와 손그림 스토리보드는 완성 전까지 공개하지 않기로 했다.
- 공모전 공식 표기에 맞춰 `Pre-Mortem(사전 실패 분석)`으로 용어를 통일했다.
- 제안서 1페이지 하단의 큰 공백을 네 단계 사고 진행선으로 재구성하고, 2페이지 결론도 의미 단위 3행으로 정리했다.
- 새 PDF를 생성해 두 페이지의 잘림·겹침·줄바꿈을 A4 렌더링으로 확인했다.

## 2026-07-17 · 완성 스토리보드 통합

- 팀원이 추가한 16:9 원본 14장을 Figma 제작 보드와 대조해 장면 순서를 확정했다.
- 두 시나리오는 같은 고립 장면 2장에서 출발해 실패 8장과 성공 8장으로 갈라지도록 구성했다.
- 원본 이미지는 `sources/storyboard-originals/`에 보존하고, 웹에는 경량화한 사본을 사용한다.
- Figma 캡션은 아직 작업 중이므로 순서와 장면 의도만 참고하고, 웹 캡션은 팀 합의문과 제안서 표현에 맞춰 편집했다.
- 사건 보관소에 실패/성공 전환 탭, 장면 확대, 키보드 접근을 추가했다.

## 2026-07-17 · 2차 가독성·상호작용·제안서 밀도 보정

- 팀원이 직접 손본 HTML의 의미 단위 줄바꿈과 표현을 보존하되, 작은 화면에서는 고정 줄바꿈이 풀리도록 편집용 클래스로 분리했다.
- 스토리보드 14장이 카드 안에서 잘리지 않도록 16:9 전체 장면 표시 방식으로 바꾸고 캡션을 이미지 아래로 분리했다.
- 사건 접수·증거·원인·프로토콜·최종 분류를 순서대로 완료해야 다음 단계가 활성화되도록 잠금과 안내 문구를 추가했다.
- 본편과 사건 보관소 사이에 스크롤 기반 전환 구간을 두어 페이지가 별도 자료 모음으로 갑자기 끊기지 않게 했다.
- 새로고침 시 본편 상단에서 다시 시작하도록 스크롤 복원을 정리했다.
- 제안서 본문을 노토 세리프, 정보 라벨을 노토 산스, 제목을 조선 계열로 통일했다.
- 제안서 1페이지는 불균형한 자동 단 구성을 명시적 양단 구성과 전폭 핵심 판정으로 바꾸고, 2페이지는 결론까지 한 흐름으로 재배치했다.
- 최종 2페이지를 다시 렌더링해 단어 분리, 빈 공간, 잘림, 겹침, 페이지 밀도를 시각 확인했다.
- 팀원 실명은 공식 표기와 공개 동의가 확인되지 않아 이번 공개본에는 넣지 않고 `TEAM INCOM 45`만 유지했다.
- 포스터 최종본은 추후 사건 보관소의 별도 전시물로 추가하는 방향을 확정했다.

## 2026-07-17 · 전환 구간·운영 원칙·팀 크레딧 보정

- 사건 보관소에 전역 섹션 여백이 중복 적용되어 생긴 남색 가로 블록을 제거했다.
- 본편과 보관소 사이의 전환 구간을 짧게 줄이고, 하나의 세로 연결선으로 장면 전환 의도를 분명히 했다.
- 제안서와 웹의 반복적인 실행 항목을 사용자의 선택권·AI 역할의 경계·기존 공간 활용이라는 운영 원칙으로 보강했다.
- 원문의 핵심 반론인 '위기상담 인력만 늘려서는 부족하다'는 진단을 제안서에 복원했다.
- 경기연구원 조사 수치 27%·41%·53%와 향후 AI 상담 의향 77%를 보도 내용과 대조했다.
- 팀원 정영운·우연정·이유빈을 사이트 말미 크레딧과 PDF 하단에 표기했다.
- 공유 미리보기의 주소·이미지·대체 설명을 절대 주소 기준으로 정리했다.

## 2026-07-17 · 최종 검수 및 경쟁작 재비교

- PDF 본문에서 편집 원칙 위반(대명사 '그는' 2곳)과 케이스바 라벨 오류('실패 영역'에 '연결' 오기재)를 발견해 수정하고 세 사본을 재생성했다.
- 저장소 루트에 `최종_제안서.pdf`를 추가해 제안서를 바로 찾을 수 있게 했다. `submission/`·`assets/` 사본과 빌드 스크립트로 동시 생성해 항상 동일하다.
- 데스크톱 폭에서 강제 줄바꿈이 문장 길이를 불균형하게 만들던 두 곳(히어로 리드, 사건 접수 첫 문단)을 재배치했다.
- `editorial-break` 숨김 기준(기존 620px)이 레이아웃 1단 전환 기준(900px)과 어긋나 태블릿 폭에서 어색한 줄바꿈이 남던 문제를 발견해 두 기준을 통일했다.
- 루트에 중복 존재하던 `STATUS.md`·`PROGRESS_LOG.md`를 정리하고 `docs/`만 남겼다.
- 경쟁작 5개(mov-hyun, jcm0314, orca-svg, DONGJUN92, BaeYoungHwan)를 최신 상태 기준으로 재조사해 강점·약점을 정리했다. 새로운 인터랙티브 툴이나 AI 생성 비주얼 제작은 마감 임박 리스크 대비 이득이 작다고 판단해 시도하지 않고, 기존 완성도를 다듬는 데 집중했다.
- 사이트가 참조하는 `href`·`src`·`poster`·`data-still` 전체 자산과 라이브 배포 상태를 재확인했다.

## 2026-07-17 · 제안서 디자인 버전 폐기 및 순수 문서형 전환

- 팀 피드백(디자인 요소가 AI 티가 많이 난다)에 따라 그래픽 레이아웃 2페이지 PDF를 폐기하고, Word로 직접 작성한 디자인 요소 없는 순수 문서형으로 전환했다.
- 새 원본을 `submission/제안서_최종본.docx`·`.pdf`로 정식 배치하고, `제안서_최종본.txt`를 최종 텍스트와 동기화했다.
- 대명사 3곳(그는/그가)을 이름으로 통일하고, 1인칭 시점이 튀던 문장을 고쳤다.
- 각주 출처가 검증되지 않은 예전 초안(세계일보 재인용)으로 남아 있던 것을 발견해, 연합뉴스 원문 기사(2026.3.17)를 직접 다시 대조해 검증된 출처로 교체했다.
- 팀명과 페이지 번호를 Word 기본 꼬리말 기능으로만 추가했다(디자인 요소 없이).
- 이전 디자인 버전 PDF와 생성 스크립트(`proposal-source/`)는 `archive/drafts/`, `archive/build-workbench/proposal-source-styled/`로 이동해 보존했다.
- `assets/final-proposal.pdf`, 저장소 루트 `최종_제안서.pdf`를 새 순수 PDF로 교체하고 해시 일치를 확인했다.

## 2026-07-18 · 포스터 통합 및 README 정비

- 팀원이 전달한 포스터 최종본(`우린, 다시 사람에게 연결한다`)을 웹 해상도로 최적화해 `assets/poster.jpg`로 배치하고, 고해상도 원본은 `sources/poster-original/`에 보존했다.
- 사건 보관소에 `EXHIBIT C · POSTER` 블록을 추가했다. 포스터의 실제 세로 비율에 맞춘 프레임을 써서 잘리는 부분 없이 전체가 보이고, 클릭하면 기존 라이트박스로 확대된다.
- 헤드리스 크롬으로 데스크톱·모바일 스크린샷을 확인해 레이아웃과 줄바꿈이 정상인지 검증했다.
- 웹에서 참조하지 않던 초기 포스터 초안(`assets/poster-wip.jpg`, 이미 `archive/supplementary/`에 보존된 것과 동일)과 초기 영상 초안(`assets/failure-film-v1.mp4`)을 정리했다. 초안 영상은 `archive/supplementary/failure-film-v1-draft.mp4`로 이동해 이력만 보존했다.
- 루트 `README.md`의 `GitHub Desktop` 절(로컬 경로와 동기화 확인 방법을 안내하던 개인 작업 환경용 문구)을 제거하고, 포스터를 바로 보기·핵심 파일·프로젝트 구조 표에 반영했다.

## 2026-07-18 · 스토리보드 갈림길 시각화, 포스터 배치 강화, README 재정비

- 실패/성공 스토리보드의 공통 시작 두 장면을 `FAILURE·01/02`·`SUCCESS·01/02`로 중복 라벨링하던 것을 `COMMON·01/02`로 통일하고, 실제로 갈라지는 지점에 시각적 구분선을 추가했다. 원화가 이미 실패는 차가운 보라, 성공은 따뜻한 금빛으로 그려져 있었다는 점을 발견해, 이 색을 탭·구분선·캡션에 그대로 반영했다(신규 색상을 만들지 않고 기존 원화 색을 노출).
- 탭 전환에 크로스페이드를 추가했다.
- 사이트 5단계를 모두 마친 완료 화면(결론 섹션의 배경이 이미 남색→금색으로 전환되는 지점)에 작은 포스터 카드를 추가해, 텍스트 결론과 포스터가 같은 순간에 같은 메시지를 전달하도록 배치했다.
- 대응 방안이 "AI가 연결자가 되어야 한다"는 당위에서 멈추고 누가 강제하는지 답하지 못한다는 점을 발견해, 재발 방지안 운영 원칙에 "04 · 도입의 강제력"(앱마켓 심사 요건화)을 추가했다. 이미 제출된 PDF 본문은 수정 대상이 아니므로 본선 대비용으로 우선 반영했다.
- 경쟁작(mov-hyun, orca-svg) README를 다시 조사해 표 위주·번호목록+대시 설명 같은 정형화된 서술을 걷어내고, 구체적 장면으로 시작하는 산문체로 README를 다시 썼다. 볼드 텍스트 바로 뒤에 조사가 붙어 렌더링이 깨지던 문장도 함께 정리했다.
- 저장소 정비: `archive/build-workbench/`에 남아 있던 폰트 점검·docx 덤프·패치 로그 등 재사용 가치가 없는 스크래치 파일 20개를 삭제했다(최종 빌드 스크립트가 있는 `proposal-source-styled/` 하위 폴더는 유지). `archive/drafts/`의 "사본" 표기 파일명을 초기초안/중간수정본/시안 A·B로 다시 이름 붙이고, 내용이 완전히 같은 `_사본_v2.docx` 중복 파일을 제거했다. `submission/`에 안내 없이 존재하던 구글폼 파일명 규칙용 PDF 사본의 존재 이유를 `submission/README.md`에 명시했다.
